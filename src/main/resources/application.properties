# Konfiguracja aplikacji

# Ustawienia email
email.host=${EMAIL_HOST:localhost}
email.port=${EMAIL_PORT:1025}
email.username=${EMAIL_USER:test@example.com}
email.password=${EMAIL_PASSWORD:password}

# Konfiguracja LLM
llm.api.url=${LLM_API_URL:http://localhost:11434}
llm.model=${LLM_MODEL:llama2}

# Ustawienia bazy danych
database.path=${DATABASE_PATH:data/emails.db}